const typed = new Typed('#typing-text', {
    strings: ['créatif.', 'passionné.', 'investi.'], // Tes textes ici
    typeSpeed: 60,
    backSpeed: 30,
    backDelay: 1500,
    startDelay: 500,
    loop: true,
    showCursor: true,
    cursorChar: '|',
});