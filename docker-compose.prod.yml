services:
  php-server:
    image: portfolio-php-fpm:latest
    build: .
    env_file:
      - .env
    depends_on:
      - mariadb-server
      - redis-server
  nginx-server:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ".:/var/www/html"
      - "./nginx/nginx-prod.conf:/etc/nginx/conf.d/default.conf"
      - "./nginx/certs:/etc/letsencrypt:ro"
    depends_on:
      - php-server
  mariadb-server:
    image: mariadb:latest
    environment:
      MYSQL_ROOT_PASSWORD: "${DB_ROOT_PASSWORD}"
      MYSQL_DATABASE: "${DB_NAME}"
      MYSQL_USER: "${DB_USER}"
      MYSQL_PASSWORD: "${DB_PASS}"
    volumes:
      - "portfolio-db-prod:/var/lib/mysql"
  redis-server:
    image: redis:alpine
volumes:
  portfolio-db-prod: